## proxy setting methods

- git
```bash
git config --global http.proxy http://Username:Password@proxy.server.com:port
git config --global https.proxy http://Username:Password@proxy.server.com:port
```
